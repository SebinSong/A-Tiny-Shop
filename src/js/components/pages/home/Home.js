import React, { memo } from 'react';

import "./Home.scss";

function Home (props) {
  return (
    <main className="page home">
      Home Page. 
      <br/>
      TBD Soon.
    </main>
  )
};

export default memo(Home);